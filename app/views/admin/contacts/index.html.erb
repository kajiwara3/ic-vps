<table class="table">
  <thead>
    <tr>
      <td><%= Contact.human_attribute_name(:partner_id)%></td>
      <td><%= Contact.human_attribute_name(:subject) %></td>
      <td><%= Contact.human_attribute_name(:cantact_status_id)%></td>
      <td><%= Contact.human_attribute_name(:created_at) %></td>
    </tr>
  </thead>
  <tbody>
    <% @contact_list.each do |contact| %>
      <tr>
        <td><%= link_to contact.partner.name, [:admin, contact] %></td>
        <td><%= contact.subject %></td>
        <td><%= contact.cantact_status.name %></td>
        <td><%= contact.created_at.try(:strftime, "%Y年%m月%d日 %H時%M分") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @contact_list %>
