<%= link_to "よくあるご質問一覧へ", :faqs %>
<ul>
  <% @faq_categories.each do |category| %>
    <li>
      <%= link_to category.name, faq_category_faqs_path(category) %>
    </li>
  <% end %>
</ul>
<%= will_paginate @faq_categories %>
